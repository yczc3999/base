# utils - 工具类

## 职责

存放通用工具函数和辅助类，不依赖具体业务逻辑。

## 文件说明

| 文件 | 说明 |
|------|------|
| `Token.ts` | JWT 工具类，提供 token 签发和验证方法 |

## 对应 PHP 项目

等价于 PHP 项目中的 `app/utils/` 或 `app/helper/` 目录，存放与框架无关的通用工具。
