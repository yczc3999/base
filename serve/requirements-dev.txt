# 开发/测试依赖（在 requirements.txt 基础上追加）
-r requirements.txt
pytest>=8.0
# Base tests/conftest.py 用 SQLite 内存库替代 PostgreSQL，需要 aiosqlite
aiosqlite>=0.20
# 钉在 0.23.x：tests/conftest.py 依赖 session 级 event_loop fixture override，
# pytest-asyncio 1.x 已移除该 override，升级会破坏既有 Base 异步测试。
pytest-asyncio>=0.23,<0.24
httpx>=0.27
