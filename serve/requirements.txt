fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
redis>=5.0
pydantic>=2.0
pydantic-settings>=2.0
bcrypt>=4.0
python-multipart>=0.0.9
openpyxl>=3.1
# schema 迁移（Alembic，ADDITIVE；种子/业务迁移仍走 python -m app.migrate）
alembic>=1.13
# Alembic 同步驱动（应用运行时用 asyncpg，Alembic 用 psycopg 3）
psycopg[binary]>=3.1
# Artifact Store 压缩（有上界：锁定主版本）
zstandard>=0.22,<1
# Artifact Store S3-compatible 共享存储 Driver（有上界：锁定主版本）
boto3>=1.43,<2
